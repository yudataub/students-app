# students-app — "זוכר שמות תלמידים"

אפליקציה למורה לזכירת שמות תלמידים: תמונות, מבחני זיכרון, סידורי ישיבה, ניקוד התנהגות.

## עובדות בסיס

- **קובץ יחיד**: כל האפליקציה ב-`index.html` (~5,800 שורות) — HTML + CSS + JS inline. אין build, אין תלויות, אין node_modules.
- **שפה**: UI בעברית, RTL. מזהים (משתנים/פונקציות) באנגלית. **תקשורת עם המשתמש: בעברית בלבד**, כולל עדכוני סטטוס קצרים. הודעות commit בעברית.
- **אחסון**: localStorage בדפדפן + סנכרון ענן אופציונלי (Firebase Firestore, פרויקט `name-2b45a`).
- **פריסה**: GitHub Pages מ-branch `main`. ריפו: `yudataub/students-app`. אתר חי: https://yudataub.github.io/students-app/
- **אין טסטים אוטומטיים** (מוסכם עם המשתמש) — בדיקות ידניות בדפדפן בלבד.
- **סופי שורות: CRLF**. אל תמיר ל-LF (סקריפט python שכותב את הקובץ חייב לשמר CRLF, אחרת ה-diff יהיה כל הקובץ).

## מבנה נתונים (localStorage)

| מפתח | תוכן |
|---|---|
| `students` | מערך תלמידים: `{id, name, className, photo, association, correct, wrong, behaviorLog}` — **כיתה קיימת רק דרך התלמידים שלה** (`getClasses()` נגזרת מ-`className`) |
| `behaviorTypes` | סוגי התנהגות לניקוד: `{id, emoji, label, kind:'good'|'bad'}` |
| `importedNamesMap` | רשימות שמות מיובאות לפי כיתה (למנגנון "שמות חסרים") |
| `deletedTombstones` | **רשת ביטחון**: מזהי תלמידים שנמחקו במפורש + timestamp (נשמר 180 יום) |
| `seating_<class>` | סידורי ישיבה |
| `firebaseConfig`, `cloudLastSync` | הגדרות ענן |

`s.photo` תמיד קיים — `PLACEHOLDER_PHOTO` (SVG inline) אם אין תמונה אמיתית.

## חוקי ברזל — סנכרון ענן (אל תשבור אותם!)

הוטמעה רשת ביטחון אחרי אובדן נתונים אמיתי (כיתה שלמה נמחקה מסנכרון דורס):

1. **עדכון מהענן לעולם לא מוחק תלמיד.** `_applyCloudData` ממזגת — תלמידים מקומיים שחסרים בענן נשמרים ומועלים חזרה (ריפוי). לעולם אל תחזיר החלפה גורפת (`students = cloudData`).
2. **מחיקה רק במפורש.** כל מסלול מחיקה מפורשת (מחיקת תלמיד, ריקון כיתה, איפוס כיתה) חייב לקרוא `recordDeletion(ids)` — זה יוצר tombstone שמסתנכרן בין מכשירים ומונע "תחייה" של תלמיד שנמחק בכוונה. אם תוסיף מסלול מחיקה חדש — אל תשכח `recordDeletion`.
3. tombstones נכללים ב-`_getCloudSnapshot()` וממוזגים ב-`_applyCloudData`.

## אזורים מרכזיים בקוד (מספרי שורות משוערים)

- `showView` / `goBack` / `navStack` — ניווט (~1478)
- `saveData()` — שמירה + תזמון סנכרון ענן (~1460)
- tombstones + `recordDeletion` (~1419)
- `renderHome()` — מסך הבית, כרטיסי כיתות (~1580)
- העלאת קבוצה + מצב התאמת תמונות: `setBulkMode`, `_findBestMatch`, `saveBulkMatches` (~2110-2260)
- מנגנון "שמות חסרים": `_normName`, `_namesMatch`, `getMissingNames` (~1500) — **שים לב: `_normName` תפוס!** לפונקציות נרמול חדשות השתמש בשם ייחודי
- ניקוד התנהגות: `showBehaviorList`, `showBehaviorDetail`, `openAddPointPicker`, `openBehaviorTypesEditor` (~2360+)
- סנכרון ענן: `_applyCloudData`, `cloudSaveNow`, `_checkAndPullCloud` (~3270-3450)
- כיתות משותפות (שיתוף בין מורים): `_listenSharedClass`, `joinSharedClass` (~5000+)

## תהליך עבודה מומלץ

1. branch חדש: `git checkout -b feature/<name>`
2. עריכות ב-`index.html`
3. בדיקת תחביר: `node -e "...new Function על כל בלוק script..."` + בדיקת כפילויות שמות פונקציות
4. בדיקה ידנית בדפדפן: שרת מקומי (`python -m http.server 8732 --directory C:/Users/User/students-app`), הרצת תרחישים דרך ה-console וה-UI
5. commit (עברית) → merge ל-main → push
6. **אימות פריסה**: `curl` לאתר החי עד שהשינוי מופיע (עד ~2 דק'). ידוע: לפעמים ה-deploy של Pages נתקע ("Deployment failed, try again later" / Queued לנצח) — הפתרון שעבד: push של commit ריק שמייצר ריצה חדשה. Re-run של הריצה התקועה לא עזר.

## באגים ידועים שלא טופלו

- **ניווט**: `goBack()` עושה pop כפול + מסכים שנטענים מחדש דוחפים כפילויות ל-`navStack` → "חזור" לפעמים מגיע למסך לא צפוי / כותרת גנרית. קיים מזמן, לא קריטי.
- שורת הפעולות בכרטיס כיתה צפופה (7-8 כפתורים) — מועמדת לתפריט "⋯" בעתיד.
- הערה: במסך הבית כפתורי "חזור"/"בית" מוסתרים **בכוונה** — זה לא באג.

## היסטוריה אחרונה (יולי 2026)

- מדור ניקוד התנהגות מלא (נקודות טובות/רעות, יומן, מיון, ניהול סוגים) — 7 משימות, עבר code review מלא
- רשת ביטחון לסנכרון ענן (מיזוג + tombstones) — אחרי שכיתה נמחקה למשתמש
- מצב "🔗 התאם תמונות לקיימים" בהעלאת קבוצה: זיהוי לפי שם קובץ (מדויק/סדר הפוך/הכלה ייחודית; דו-משמעי נשאר ריק), תיבת "עדכן רק תלמידים שאין להם תמונה" (ברירת מחדל: מסומנת), הגנת כפילויות
- ⚠️ עבודה משתי שיחות במקביל על הקובץ גרמה כמעט-התנגשות (שתי פונקציות `_normName`) — לפני הוספת פונקציה חדשה בדוק שהשם פנוי, ועדיף לא לעבוד משתי שיחות במקביל

## מסמכים נוספים בריפו

- `docs/superpowers/specs/` — מסמכי אפיון (למשל ניקוד התנהגות)
- `docs/superpowers/plans/` — תוכניות מימוש מפורטות; טובות גם כ-checklist לבדיקות רגרסיה ידניות
