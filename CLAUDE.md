# students-app — "זוכר שמות תלמידים"

אפליקציה למורה לזכירת שמות תלמידים: תמונות, מבחני זיכרון, סידורי ישיבה, ניקוד התנהגות. **שתי גרסאות חיות** (ר' סעיף ייעודי למטה) — קרא את זה לפני כל עריכה.

## עובדות בסיס

- **קובץ יחיד**: כל האפליקציה ב-`index.html` (~5,900 שורות) — HTML + CSS + JS inline. אין build, אין תלויות, אין node_modules.
- **שפה**: UI בעברית, RTL. מזהים (משתנים/פונקציות) באנגלית. **תקשורת עם המשתמש: בעברית בלבד**, כולל עדכוני סטטוס קצרים. הודעות commit בעברית.
- **אחסון**: localStorage בדפדפן (~5MB) + סנכרון ענן אופציונלי (Firebase Firestore, פרויקט `name-2b45a`). בגרסה המקומית (`desktop/`) האחסון הוא קובץ בדיסק — בלי מגבלת גודל, בלי ענן.
- **פריסה**: GitHub Pages מ-branch `main`, שורש הריפו. ריפו: `yudataub/students-app`. אתר חי: https://yudataub.github.io/students-app/ — **חשוב**: push ל-GitHub ≠ "עובד בכל מקום". רק ה-`index.html` הראשי רץ בפועל דרך GitHub Pages (שירות אחסון+הרצה נפרד). הגרסה המקומית (`desktop/`) היא רק **קוד מגובה** ב-git — היא לא "רצה" באף מקום עד שמישהו מריץ אותה פיזית על מחשב ספציפי (`הפעל.bat`), וזה צריך לקרות בנפרד בכל מחשב שרוצים להשתמש בה עליו.
- **אין טסטים אוטומטיים** (מוסכם עם המשתמש) — בדיקות ידניות בדפדפן בלבד (Claude Browser: `preview_start` + `javascript_tool` לסימולציית תרחישים, לפני/במקום קליק אמיתי).
- **סופי שורות: CRLF**. אל תמיר ל-LF (סקריפט python שכותב את הקובץ חייב לשמר CRLF, אחרת ה-diff יהיה כל הקובץ).

## ⚠️ שתי גרסאות = שני קבצים כמעט-זהים (הכי חשוב לדעת!)

מאז אוגוסט 2026 יש **`desktop/index.html`** — עותק כמעט-מדויק של `index.html` הראשי, שרץ מקומית על מחשב המשתמש (ר' `desktop/CLAUDE.md` להסבר המלא על איך זה עובד — שכבת אחסון מוחלפת, Firebase מנוטרל).

**כלל ברזל: כל שינוי בפיצ'ר קיים ב-`index.html` חייב להיכנס גם ל-`desktop/index.html`**, אלא אם השינוי ספציפי ל-Firebase/ענן/שיתוף (זה כבר שונה בכוונה בין הגרסאות — הענן מנוטרל לגמרי ב-desktop). דרך עבודה בטוחה: תעשה את השינוי ב-`index.html` קודם ותבדוק, ואז תעתיק **בדיוק אותו diff** ל-`desktop/index.html`. תמיד תריץ שוב בדיקת תחביר על **שני** הקבצים (ר' "תהליך עבודה" למטה).

זה גם אומר: אם המשתמש מדווח על באג ולא ברור אם הוא בגרסה החיה או המקומית — תשאל, או תבדוק את שתיהן.

## מבנה נתונים (localStorage / data.json בדסקטופ)

| מפתח | תוכן |
|---|---|
| `students` | מערך תלמידים: `{id, name, className, photo, extraPhotos, association, correct, wrong, behaviorLog}` — **כיתה קיימת רק דרך התלמידים שלה** (`getClasses()` נגזרת מ-`className`) |
| `behaviorTypes` | סוגי התנהגות לניקוד: `{id, emoji, label, kind:'good'|'bad'}` |
| `importedNamesMap` | רשימות שמות מיובאות לפי כיתה (למנגנון "שמות חסרים") |
| `deletedTombstones` | **רשת ביטחון**: מזהי תלמידים שנמחקו במפורש + timestamp (נשמר 180 יום) |
| `seating_<class>` | סידורי ישיבה |
| `firebaseConfig`, `cloudLastSync` | הגדרות ענן (לא קיים/רלוונטי בגרסת דסקטופ) |

- `s.photo` תמיד קיים — `PLACEHOLDER_PHOTO` (SVG inline) אם אין תמונה אמיתית.
- `s.extraPhotos` (מערך, יכול להיות ריק) — תמונות נוספות **של אותו תלמיד** (למשל תמונה לא ברורה שהתלמיד/מורה השלימו עם עוד תמונה). במבחנים נבחרת אקראית אחת מ-`[photo, ...extraPhotos]` בכל הופעה (`_quizPhotoFor`) — אבל זו עדיין תשובה נכונה אחת, כי זה רשומת תלמיד יחידה.

## חוקי ברזל — סנכרון ענן (אל תשבור אותם!)

הוטמעה רשת ביטחון אחרי אובדן נתונים אמיתי (כיתה שלמה נמחקה מסנכרון דורס):

1. **עדכון מהענן לעולם לא מוחק תלמיד.** `_applyCloudData` ממזגת — תלמידים מקומיים שחסרים בענן נשמרים ומועלים חזרה (ריפוי). לעולם אל תחזיר החלפה גורפת (`students = cloudData`).
2. **מחיקה רק במפורש.** כל מסלול מחיקה מפורשת (מחיקת תלמיד, ריקון כיתה, איפוס כיתה) חייב לקרוא `recordDeletion(ids)` — זה יוצר tombstone שמסתנכרן בין מכשירים ומונע "תחייה" של תלמיד שנמחק בכוונה. אם תוסיף מסלול מחיקה חדש — אל תשכח `recordDeletion`.
3. tombstones נכללים ב-`_getCloudSnapshot()` וממוזגים ב-`_applyCloudData`.
4. **`photo` ו-`extraPhotos` תמיד מוצאים מהמסמך הראשי ב-Firestore** ונשמרים ב-subcollection `photos` נפרד (מסמך לכל תלמיד) — כדי לא לחרוג ממגבלת 1MB של מסמך Firestore. אם מוסיפים שדה תמונה נוסף בעתיד — תזכור לעדכן גם את `cloudSaveNow` (הכתיבה) וגם את `_readCloudData` (הקריאה) באותו אופן.

## אזורים מרכזיים בקוד (מספרי שורות משוערים, `index.html`)

- `showView` / `goBack` / `navStack` — ניווט (~1485)
- `saveData()` — שמירה + תזמון סנכרון ענן (~1476)
- tombstones + `recordDeletion` (~1420)
- `exportFullBackup()` — ייצוא כל הנתונים ל-`backup.json` לגרסה המקומית (~1483)
- `renderHome()` — מסך הבית, כרטיסי כיתות (~1590)
- העלאת קבוצה: `setBulkMode`, `_findBestMatch` (מצב התאמה), `_mergeDuplicateBulkItems`/`_stripBulkDupSuffix` (מיזוג תמונות כפולות במצב יצירה), `saveBulk`/`saveBulkMatches` (~2200-2340)
- מנגנון "שמות חסרים": `_normName`, `_namesMatch`, `getMissingNames` (~1500) — **שים לב: `_normName` תפוס!** לפונקציות נרמול חדשות השתמש בשם ייחודי (למשל מיזוג הכפילויות למעלה השתמש ב-`_stripBulkDupSuffix`, לא ב-`_normName`)
- מבחנים: `startQuiz`/`renderQuestion`, `startRQuiz`/`renderRQuestion`, `startMQuiz`/`renderMQuestion`, כולם דרך `_quizPhotoFor(s)` לבחירת תמונה (~2840+)
- ניקוד התנהגות: `showBehaviorList`, `showBehaviorDetail`, `openAddPointPicker`, `openBehaviorTypesEditor` (~2400+)
- סנכרון ענן: `_applyCloudData`, `cloudSaveNow`, `_readCloudData`, `_checkAndPullCloud` (~3350-3600)
- כיתות משותפות (שיתוף בין מורים): `_listenSharedClass`, `joinSharedClass` (~5000+)

## תהליך עבודה מומלץ

1. branch חדש: `git checkout -b feature/<name>`
2. עריכות ב-`index.html` — **ואז אותו diff ב-`desktop/index.html`** אם זה פיצ'ר לא-ספציפי-לענן (ר' הסעיף למעלה)
3. בדיקת תחביר על **שני** הקבצים:
   ```bash
   node -e "const fs=require('fs');['index.html','desktop/index.html'].forEach(p=>{const h=fs.readFileSync(p,'utf8');[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)].forEach(m=>new Function(m[1]));console.log(p,'OK');});"
   ```
   + בדיקת כפילויות שמות פונקציות/const (שני `const` באותו שם בסקופ העליון = SyntaxError ששובר את כל האפליקציה — קרה כבר פעם אחת בטעות)
4. בדיקה ידנית בדפדפן: שרת מקומי (Claude Browser `preview_start` עם `name: "students-app"` מ-`.claude/launch.json`, פורט 8732), הרצת תרחישים דרך `javascript_tool` (לפני קליקים אמיתיים) — כולל ניקוי `localStorage.clear()` בסוף הבדיקה כדי לא להשאיר נתוני בדיקה
5. commit (עברית, מפורט על מה נבדק) → merge ל-main → push
6. **אימות פריסה לאתר החי**: `curl` ל-URL עם cache-buster עד שהשינוי מופיע (עד ~2 דק', אפשר עם `run_in_background`). ידוע: לפעמים ה-deploy של GitHub Pages נתקע ("Deployment failed, try again later" / Queued לנצח) — הפתרון שעבד: push של commit ריק שמייצר ריצה חדשה. Re-run של הריצה התקועה **לא** עזר.
7. **הגרסה המקומית לא מתעדכנת לבד** — אחרי push, אם המשתמש רוצה את השינוי גם ב-`desktop/`, צריך `git pull` בפועל על אותו מחשב (או שאתה, Claude, מריץ את זה מטעמו אם אתה עובד באותה סביבה).

## באגים ידועים שלא טופלו

- **ניווט**: `goBack()` עושה pop כפול + מסכים שנטענים מחדש דוחפים כפילויות ל-`navStack` → "חזור" לפעמים מגיע למסך לא צפוי / כותרת גנרית. קיים מזמן, לא קריטי.
- שורת הפעולות בכרטיס כיתה צפופה (7-8 כפתורים) — מועמדת לתפריט "⋯" בעתיד.
- הערה: במסך הבית כפתורי "חזור"/"בית" מוסתרים **בכוונה** — זה לא באג.
- תוסף אפשרי עתידי (לא בנוי): הוספת/ניהול `extraPhotos` ידנית לתלמיד קיים מהפרופיל (כרגע רק אוטומטי דרך זיהוי קבצי א/ב-1/2 בהעלאת קבוצה במצב "צור תלמידים חדשים").

## היסטוריה אחרונה

**יולי 2026:**
- מדור ניקוד התנהגות מלא (נקודות טובות/רעות, יומן, מיון, ניהול סוגים) — 7 משימות, עבר code review מלא
- רשת ביטחון לסנכרון ענן (מיזוג + tombstones) — אחרי שכיתה נמחקה למשתמש
- מצב "🔗 התאם תמונות לקיימים" בהעלאת קבוצה + תיבת "עדכן רק תלמידים שאין להם תמונה"

**אוגוסט 2026:**
- **`desktop/`** — גרסה מקומית שרצה על המחשב (Python stdlib, פורט 8733, `localhost` בלבד), פותרת את מגבלת 5MB של localStorage. ר' `desktop/CLAUDE.md` לפרטים המלאים. מסמך אפיון: `docs/superpowers/specs/2026-08-12-desktop-local-app-design.md`.
- כפתור "📦 ייצוא גיבוי מלא למחשב" בהגדרות (באתר הראשי) לייבוא חד-פעמי לגרסה המקומית.
- תיקון: פוקוס אוטומטי על שדה "כיתה חדשה" כשהוא נחשף (בעיית מובייל אפשרית — לא שוחזרה בבדיקה אוטומטית, המשתמש אישר שהתיקון עבד בפועל).
- **`extraPhotos`** — תלמיד יכול להיות עם כמה תמונות. בהעלאת קבוצה במצב "צור תלמידים חדשים", קבצים עם אותו שם וסיומת מבדילה בלבד ("דוד כהן א"/"דוד כהן ב", או "1"/"2") מזוהים כאותו תלמיד ומוזגים לרשומה אחת, במקום ליצור שני תלמידים. במבחנים נבחרת תמונה אקראית מכל התמונות הזמינות של התלמיד בכל הופעה.
- ⚠️ עבודה משתי שיחות במקביל על הקובץ גרמה כמעט-התנגשות בעבר (שתי פונקציות `_normName`) — לפני הוספת פונקציה חדשה בדוק שהשם פנוי, ועדיף לא לעבוד משתי שיחות במקביל.

## מסמכים נוספים בריפו

- `docs/superpowers/specs/` — מסמכי אפיון (ניקוד התנהגות, גרסה מקומית)
- `docs/superpowers/plans/` — תוכניות מימוש מפורטות; טובות גם כ-checklist לבדיקות רגרסיה ידניות
- `desktop/CLAUDE.md` — הוראות ייעודיות לעבודה על הגרסה המקומית (שכבת האחסון, פורמט `data.json`, מה שונה מהגרסה הראשית)
